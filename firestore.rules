rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permettre la lecture et l'écriture pour tous les utilisateurs
    // (Pour commencer, nous utilisons des règles ouvertes)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
